[project]
name = "quickprint-pi-agent"
version = "0.1.0"
description = "Raspberry Pi print agent for QuickPrint"
requires-python = ">=3.13"
readme = "README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
